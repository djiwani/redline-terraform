apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    # This annotation is what links the service account to the IAM role
    # IRSA uses this to grant the pod AWS permissions
    eks.amazonaws.com/role-arn: {{ .Values.irsaRoleArn }}
